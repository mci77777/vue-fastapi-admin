# GymBro AI Endpoint 所需的最小化配置示例
# 复制为 .env 后按环境替换占位内容

# OpenAI 兼容服务基础地址（可指向自建反向代理）
OPENAI_API_BASE_URL=https://api.openai.com/v1/chat/completions
# 默认使用的上游模型 ID
OPENAI_API_MODEL=gpt-4o-mini
# 调用上游模型所需的密钥
OPENAI_API_KEY=sk-your-openai-key
# 服务端向上游请求的超时时间（秒）
OPENAI_TIMEOUT_SECONDS=60
# 默认系统 Prompt JSON 文件路径（支持相对路径）
OPENAI_PROMPT_PATH=docs/prompts/standard.json
